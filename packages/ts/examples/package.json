{
  "name": "@signus/temporal-kit-examples",
  "version": "0.0.0",
  "description": "Runnable examples for temporal-kit. Not published.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src"
  },
  "dependencies": {
    "@signus/temporal-kit-core": "workspace:*",
    "@signus/temporal-kit-patterns": "workspace:*",
    "@signus/temporal-kit-testing": "workspace:*"
  }
}
